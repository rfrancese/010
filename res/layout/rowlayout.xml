<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:padding="6dip" >

      
    <ImageView
        android:id="@+id/scudettoSquadraCasa"
        android:layout_width="wrap_content"
        android:layout_height="fill_parent"
        android:layout_alignParentBottom="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="6dip"
        android:contentDescription="TODO"
        android:src="@drawable/ic_launcher" />

    <TextView
        android:id="@+id/squadraCasa"
        android:layout_width="100dip"
        android:layout_height="26dip"
        android:layout_alignBottom="@+id/scudettoSquadraCasa"
        android:layout_toRightOf="@+id/scudettoSquadraCasa"
        android:ellipsize="marquee"
        android:singleLine="true"
        android:text="Description"
        android:textSize="12sp" />
       
     <TextView
        android:id="@+id/squadraFuori"
        android:layout_width="100dip"
        android:layout_height="26dip"
        android:layout_alignBottom="@+id/scudettoSquadraFuori"
        android:layout_toLeftOf="@+id/scudettoSquadraFuori"
        android:ellipsize="marquee"
        android:singleLine="true"
        android:text="Description"
        android:textSize="12sp" />

     <ImageView
         android:id="@+id/scudettoSquadraFuori"
         android:layout_width="wrap_content"
         android:layout_height="fill_parent"
         android:layout_alignParentBottom="true"
         android:layout_alignParentRight="true"
         android:layout_marginRight="14dp"
         android:contentDescription="TODO"
         android:src="@drawable/ic_launcher" />

    </RelativeLayout>